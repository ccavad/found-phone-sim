import { createTheme } from "@shopify/restyle";

const theme = createTheme({
  colors: {
    background: "#000",
    text: "#fff",
  },
  heading: {},
  spacing: {
    s: 8,
    m: 16,
    l: 24,
  },
});

export default theme;
